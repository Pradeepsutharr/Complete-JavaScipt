// console.log("pradeep");
let fname = "pradeep"; //can be re-assigned but cant re-declare
fname = "aksh"; // changing value its allowed in let
// let fname = "himanshu"; //not allowed in let, error
console.log(fname);

var lname = "suthar"; //can be re-declare and re-assigned
var lname = "kumar"; //re-declaring its allowed in var
lname = "chauhan"; // re-assigning its also allowed in var
console.log(lname);

const age = 23; // couldnt be  changed nor re-declare neither re-assign
// age = 22;   //not allowed in const
console.log(age);

city = "sirohi";
console.log(city);
